# Windows 10 / 11 验收清单 · 0.2.0

前端 Chromium 验收不等于 Windows 原生窗口行为已验证。构建与自动化测试结果见 VERIFICATION.md；以下交互项目请使用 0.2.0 的 EXE 在 Windows 10 / 11 上验收。

1. 首次启动：无预置地址；填写 Keeper 地址与登录密码；保存前成功验证；关闭后再启动可恢复。旧版配置能读取。
2. 图标：窗口、托盘、EXE 使用彩色 Keeper 图标；默认浅色，深色切换并重启保持。
3. 代理：HTTP、SOCKS5、SOCKS5H 按配置使用。无效地址显示错误且不覆盖旧配置；清空后直连。代理中断时不静默直连。
4. 字体：已安装鸿蒙黑体时优先使用；未安装时微软雅黑正常显示，不出现方块或溢出。自定义已安装字体改变圆球与增量文字，详情不变。
5. 默认圆球显示北京时间今日全局总量，输入 / 输出每 2 秒显示两次成功采样的差。无新增显示 0，首采样显示 —，不能把一整天用量当实时值。
6. 对照 Keeper 健康结论，断线时显示离线，恢复后增量与实际间隔正确。北京时间跨日后不出现负数或异常巨大跳变。
7. 置顶窗口不在任务栏重复出现，悬停展开不抢原应用键盘焦点。光标从球到面板、穿过间隔、操作筛选及滚动时不闪退；离开两者后收起。
8. 拖动不误打开详情；左右边缘吸附；重启恢复位置。多显示器含负坐标、100% / 125% / 150% / 200% DPI 下文字和位置正确。切换显示器或拔掉屏幕后仍能找到悬浮球。
9. 详情日期 / Key 筛选不改变圆球的全局数据；自定义起止日期有效；五个指标页和四个账户子页均可访问。错误原因可展开，长表格可横向滚动。
10. 设置、托盘隐藏 / 显示 / 退出、重复启动、锁屏恢复和睡眠恢复正常；退出后进程与托盘图标清除。
11. 注册表 `HKCU\Software\KeeperUsagePanel` 无明文 Keeper 密码 / 代理认证信息。不启动监听端口或远程服务，不请求 quota refresh。
12. 安装包缺少 WebView2 时提示并安装运行时；便携版需已有 WebView2。文件未做代码签名，可先核对 SHA256。

反馈时请附系统版本、缩放比例、具体操作和截图。不要发送密码、代理凭据或完整 API Key。
