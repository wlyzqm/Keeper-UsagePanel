# Windows 10 / 11 初版验收

使用 `KeeperUsagePanel.exe`，不需要安装远端服务或 .NET。

1. **首次配置**：运行后出现 Keeper 地址配置；填写 Windows 能访问的完整 Keeper 地址（保留 `/usage`）和登录密码。确认保存后能连接。
2. **注册表**：`HKCU\Software\KeeperUsagePanel` 出现 Endpoint 等值；重启应用沿用地址。勾选记住密码时只出现二进制 ProtectedPassword，不应存在明文 Password。
3. **悬浮球**：首次输入/输出为 `—`；之后默认每 2 秒显示两次成功采样之间的增加值。无新用量时为 `+0`。今日总量与 Keeper 同时段一致。
4. **交互**：鼠标停球上打开面板；移到面板仍保持；离开两者后收起。下拉框、日期选择、滚动表格时不误关闭。单纯悬停不抢正在输入应用的焦点。
5. **筛选**：切换某 Key、近 30 天、本月；只影响面板，不改变球的全局统计。账户额度和错误的筛选边界应有说明。
6. **账户**：检查概览、额度历史、请求明细、错误事件四个子页签，必要时翻页。缺失或不支持的数据不得显示成零。
7. **多屏**：拖到副屏，在 100% / 150% / 200% 缩放下检查球与面板位置；边缘展开不得完全跑出屏幕。移除副屏后仍能找到球。
8. **断线与恢复**：断开网络后显示离线；恢复后间隔按实际时间计算，包含断线期间累计增长，不冒充“两秒速率”。
9. **后台行为**：测试托盘隐藏 / 恢复 / 退出、锁屏 / 休眠恢复、可选开机启动。程序不要求管理员权限。

普通置顶不承诺覆盖 UAC 安全桌面或所有独占全屏游戏。初版未签名，可能出现 SmartScreen 提示。反馈时请提供系统版本、屏幕缩放、具体操作和截图；不要发送登录密码。
