# Windows 10 / 11 验收清单 · 0.4.3

前端 Chromium 验收不等于 Windows 原生窗口行为已验证。历史构建与自动化测试结果见 VERIFICATION.md；以下交互项目请使用 0.4.3 的 EXE 在 Windows 10 / 11 上验收。

1. 首次启动：无预置地址；填写 Keeper 地址，分别用管理员密码与有效 sk 登录；保存前成功验证；关闭后再启动可恢复。旧版配置能读取。
2. 图标：窗口、托盘、EXE 使用彩色 Keeper 图标；默认浅色，深色切换并重启保持。
3. 代理：HTTP、SOCKS5、SOCKS5H 按配置使用。无效地址显示错误且不覆盖旧配置；清空后直连。代理中断时不静默直连。
4. 字体：已安装鸿蒙黑体时优先使用；未安装时微软雅黑正常显示，不出现方块或溢出。自定义已安装字体改变悬浮块与增量文字，详情不变。
5. 管理员默认显示北京时间今日全局总量；sk 或已选 Key owner 显示该 Key 今日总量，输入 / 输出每 2 秒显示两次成功采样的差。两路同时无新增时暂留上一组结果，连续零用量达到约 16 秒后归零；任一路新增则重置计时并显示本次一组结果。详情仍显示实际 0。首采样 / 重置 / 断线显示 —，不能把一整天用量当实时值。
6. 对照 Keeper 健康结论，sk / 所选 Key 不再计入五小时前但仍在 Activity 顶层约 24 小时窗口内的失败；断线时显示离线，恢复后增量与实际间隔正确。北京时间跨日后不出现负数或异常巨大跳变。
7. 悬浮块和详情不在任务栏重复出现；设置窗为非置顶独立窗口，可在任务栏 / Alt+Tab 切换，悬停展开不抢原应用键盘焦点。光标从球到面板、穿过间隔、操作筛选及滚动时不闪退；离开两者后收起。
8. 拖动不误打开详情；左右边缘吸附；重启恢复位置。多显示器含负坐标、100% / 125% / 150% / 200% DPI 下文字和位置正确。切换显示器或拔掉屏幕后仍能找到悬浮球。
9. 详情日期筛选不改变悬浮块的今日口径；管理员选 Key owner 后，两处均为该 Key 的指标。快速切换 A → B → 全部，旧响应不可串数，增量及暂留清空。关闭 / 悬停 / 点击重新展开仍保留本次会话的 Key；自定义日期有效。管理员全部 Key 可访问五页与四个账户子页；选 Key 时不显示无法归属的认证账户。错误原因可展开，长表格可横向滚动。
10. 设置、托盘隐藏 / 显示 / 退出、重复启动、锁屏恢复和睡眠恢复正常；退出后进程与托盘图标清除。
11. 注册表 `HKCU\Software\KeeperUsagePanel` 无明文 Keeper 密码 / 完整 sk / 代理认证信息。不启动监听端口或远程服务，不请求 quota refresh。
12. 布局：悬浮块 200 × 58 DIP，无“今日”及间隔文字；Token 在总量下方，输入 / 输出数值紧邻标签。主指标字号一致；配额大数字不拆行，长名称可查看提示。静态文字无法鼠标选中，输入框仍可选中复制粘贴。
13. 安装包缺少 WebView2 时提示并安装运行时；便携版需已有 WebView2。文件未做代码签名，可先核对 SHA256。

14. sk 模式只有自身总览及日期筛选，无 Key owner 切换器和管理员页签；对照 Keeper sk 页面总量 / 成本。错误或停用 sk 不保存；从管理员切换 sk 时必须重新输入对应凭据，不误用旧密码。关闭记住凭据后重启需要重新输入。
15. 同时验证悬停和点击展开；点击不拖动时鼠标保持普通样式。连续运行、隐藏 / 显示多次、锁屏 / 睡眠恢复后仍可展开；没有「北京时间今日」系统悬停提示。
16. 设置窗退到后台或最小化后，悬停仍可展开详情；点击入口不受设置窗影响。设置窗打开后切换到浏览器 / 记事本，复制密码或 sk 再返回粘贴；输入过程中详情不弹出抢焦点；重复打开设置不得清空正在填写的内容。保存成功不会把已隐藏的其他应用拉到前台。

17. 顶栏顺序为连接状态、用量控制台、CPA 控制台、文字设置按钮。用量控制台打开配置地址；管理员 CPA 入口遵循 Keeper 的公开地址（含子路径），均在默认浏览器打开，不替换浮窗页面、不自动传递密码或 sk。sk 下 CPA 按钮禁用并提示权限原因。无默认浏览器或无效 CPA 地址时显示可读错误。

反馈时请附系统版本、缩放比例、具体操作和截图。不要发送密码、代理凭据或完整 API Key。
